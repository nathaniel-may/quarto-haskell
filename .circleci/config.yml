version: 2.1
jobs:
  build:
    docker:
      - image: fpco/stack-build:lts
    steps:
      - checkout
      - restore_cache:
          name: Restore Cached Dependencies
          key: {{ checksum "stack.yaml" }}-{{ checksum "package.yaml" }}-{{ checksum "liquid-quarto.cabal" }}
      - run:
          name: Resolve/Update Dependencies
          command: stack --no-terminal setup
      - run:
          name: Run tests
          command: stack --no-terminal test
      - save_cache:
          name: Cache Dependencies
          key: {{ checksum "stack.yaml" }}-{{ checksum "package.yaml" }}-{{ checksum "liquid-quarto.cabal" }}
          paths:
            - "/root/.stack"
            - ".stack-work"
